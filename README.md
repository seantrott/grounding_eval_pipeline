# Evaluation of grounded and ungrounded models

Contains code for evaluating text-based and visually grounded language models. Currently, the evaluation code is set up to evaluate the Pythia suite: see `src/pythia_demo`.